CREATE TABLE users (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    email VARCHAR2(150) NOT NULL,
    password VARCHAR2(150) NOT NULL,
    first_name VARCHAR2(150) NOT NULL,
    PRIMARY KEY (id),
    UNIQUE (email)
);

CREATE TABLE notes (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    data VARCHAR2(10000) NOT NULL,
    date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    user_id NUMBER NOT NULL,
    PRIMARY KEY (id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);


  CREATE TABLE TLEAVES 
   (	ID_NO char(7) , 
	SNO NUMBER(5,0), 
	LEAVE_TYPE NUMBER(4,0), 
	APPLIEDDATE DATE, 
	FROMDATE DATE, 
	FROMSLOT NUMBER(1,0), 
	TODATE DATE, 
	TOSLOT NUMBER(1,0), 
	SANC_IDNO CHAR(7 BYTE), 
	SANC_FROM DATE, 
	SANC_TO DATE, 
	AUTH_IDNO CHAR(7 BYTE), 
	AUTH_FROM DATE, 
	AUTH_TO DATE, 
	REFSNO NUMBER(5,0), 
	REASON VARCHAR2(50 BYTE), 
	ACTINACT NUMBER(1,0), 
	ENTRY_ID CHAR(7 BYTE), 
	ENTRY_DATE DATE, 
	APPLIEDDAYS NUMBER(4,1), 
	LOCK_UNLOCK NUMBER(1,0), 
	GROUP_LEAVE_TYPE NUMBER(4,0), 
	GROUP_LEAVE_DAYS NUMBER(4,1), 
	GROUP_SNO NUMBER(5,0), 
	REDIRECTID CHAR(7 BYTE), 
	SANC_DATE DATE, 
	AUTH_DATE DATE, 
	COMMENTS VARCHAR2(100 BYTE), 
	RECNOS NUMBER(5,0), 
	CANCEL_IDNO CHAR(7 BYTE), 
	CANCEL_DATE DATE, 
	CANCEL_REASON VARCHAR2(50 BYTE), 
	MAIL_SEND_REMARKS VARCHAR2(300 BYTE)
   )